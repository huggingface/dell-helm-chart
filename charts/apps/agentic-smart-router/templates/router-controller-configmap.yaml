apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "llm-router.fullname" . }}-router-controller-config
  labels:
    {{- include "llm-router.labels" . | nindent 4 }}
    app.kubernetes.io/component: router-controller
data:
  config.yaml: |-
{{ .Values.routerController.customConfig | indent 4 }}
